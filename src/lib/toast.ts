// Deprecated: Previously provided a wrapper for Sonner toast messages.
// This file is kept for backward compatibility during migration,
// but now components should use `sonner`'s `toast` directly.
export {}; // no-op export to avoid import errors while removing wrapper usage
